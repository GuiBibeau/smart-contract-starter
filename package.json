{
  "name": "project",
  "description": "A dapp project!",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build --include-dependencies --no-deps",
    "dev": "turbo run dev",
    "deploy": "turbo run deploy"
  },
  "devDependencies": {
    "turbo": "^1.4.3"
  }
}
