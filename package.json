{
  "name": "wen-paycheck",
  "description": "A paycheck service for ethereum",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build --scope=app --include-dependencies --no-deps",
    "dev": "turbo run dev",
    "deploy": "turbo run deploy"
  },
  "devDependencies": {
    "@types/node": "^18.7.3",
    "turbo": "^1.4.3"
  }
}
